{
  "name": "pvp-battle-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for PvP Battle Game on Base",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run script/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run script/deploy.js --network baseSepolia",
    "deploy:mainnet": "hardhat run script/deploy.js --network baseMainnet",
    "verify": "hardhat verify",
    "coverage": "hardhat coverage",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/hardhat-upgrades": "^3.0.0",
    "hardhat": "^2.19.0",
    "hardhat-gas-reporter": "^1.0.9"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "@chainlink/contracts": "^0.8.0",
    "dotenv": "^16.3.1"
  }
}
